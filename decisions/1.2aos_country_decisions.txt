country_decisions = {

	aos_fool_study_decisions = {
		
		potential = {
			is_ai = no
			current_ruler = {
				NOT = {
					has_trait = studying_trait
				}
			}
			OR = {
				has_country_modifier = zhijunbushan_buff
				has_country_modifier = zhizhengbushan_buff
				has_country_modifier = unwelcome_ruler_buff
				has_country_modifier = negative_ruler_buff
			}
		}
		
		allow = {
			political_influence >= 5
			treasury >= 50
		}
		
		effect = {
			add_political_influence = -5
			add_treasury = -50
			trigger_event = {
				id =  character_study_events.1
			}
		}
		
	}

	aos_hold_a_state_banquet = {
		
		potential = {
			NOT = {
				has_variable = country_serve_banquet
			}
			is_ai = no
		}
		
		allow = {
			political_influence >= 3
			treasury >= 100
		}
		
		effect = {
			add_political_influence = -3
			add_treasury = -100
			current_ruler = {
				add_popularity = 3
			}
			trigger_event = {
				id = serve_a_banquet.1
			}
		}
		
	}

	aos_go_hunting = {
		
		potential = {
			NOT = {
				has_variable = country_go_hunting
			}
			is_ai = no
		}
		
		allow = {
			political_influence >= 5
			treasury >= 120
		}
		
		effect = {
			add_political_influence = -5
			add_treasury = -120
			current_ruler = {
				add_popularity = 5
			}
			trigger_event = {
				id = go_hunting.1
			}
		}
		
	}

	aos_view_help = {
		
		potential = {
			is_ai = no
		}
		
		effect = {
			trigger_event = {
				id = aos_game_helper.1
			}
		}
		
	}

	aos_retrieve_lost_treasure = {
		potential = {
			has_variable = china_is_unified
			country_culture_group = chinese
		}

		effect = {
			trigger_event = {
				id = aos_retrieve_lost_treasure.1
			}
		}
	}

	aos_remove_negative_trait = {
		potential = {
			is_ai = no
			current_ruler = {
				OR = {
					has_trait = cruel
					has_trait = abrasive
				}
			}
		}

		allow = {
			current_ruler = {
				finesse > 9
			}
		}

		effect = {
			trigger_event = {
				id = aos_remove_negative_trait.1
			}
		}

	}

	aos_zhuhou_qintunjunxian = {
		potential = {
			is_subject = yes
			country_culture_group = chinese
			current_date > 550.1.1
			NOT = {
				has_variable = jinqiqintunjunxian
			}
			overlord = {
				NOT = {
					has_variable = jiaqiangtianwei
				}
			}
		}

		allow = {
			custom_tooltip = {
				text = "aos_zhuhou_qintunjunxian_text1"
				political_influence > 10
				OR = {
					overlord = {
						OR = {
							has_variable = regent_variable
							has_civil_war = yes
						}
					}
					current_ruler = {
						OR = {
							has_trait = proud
							has_trait = abrasive
							has_trait = crafty
							has_trait = sceptical
							has_trait = arbitrary
							has_trait = cruel
							has_trait = vengeful
							has_trait = selfish
							has_trait = deceitful
							has_trait = shrewd
							has_trait = gluttonous
							has_trait = ambitious
							has_trait = sarcastic
							has_trait = miserly
							has_trait = jealous
						}
					}
				}
			}
			current_ruler = {
				martial > 4
			}
			has_subject_loyalty <= 68
			NOT = {
				current_ruler = {
					has_trait = guileless
					has_trait = righteous
					has_trait = chaste
					has_trait = humble
					has_trait = honest
					has_trait = generous
					has_trait = submissive
				}
			}
		}

		effect = {
			trigger_event = {
				id = zhuhouguo_events.1
			}
			add_political_influence = -10
			set_variable = {
				name = jinqiqintunjunxian
				days = {100 280}
			}
		}

		ai_will_do = {
			factor = 1000
		}
	}

	aos_tianzi_jiaqiangtianwei = {
		potential = {
			NOT = {
				has_variable = jiaqiangtianwei
			}
			is_overlord = yes
			is_subject = no
			country_culture_group = chinese
		}

		allow = {
			political_influence >= 35
			current_ruler = {
				finesse > 5
				is_adult = yes
			}
		}

		effect = {
			add_political_influence = -35
			set_variable = {
				name = jiaqiangtianwei
				days = 3650
			}
		}
	}
}