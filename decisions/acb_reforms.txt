country_decisions = {
	
	chu_eliminate_maladministration = {
		
		potential = {
			has_country_modifier = chuhuaiwang
		}

		allow = {
			char:1370 = {
				is_alive = no
			}
			current_ruler = {
				charisma >= 8
				martial >= 8
			}
		}
		
		effect = {
			remove_country_modifier = chuhuaiwang
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	chu_move_capital_to_shouchun = {
		potential = {
			tag = CHU
			NOT = {owns = 8972}
			capital_scope = {
				NOT = {province_id = 9049}
			}
		}

		allow = {
			owns = 9049
		}
		
		effect = {
			set_capital = p:9049
			p:9049 = {
				capital_formable_small_effect = yes
				formable_capital_modifier_normal_effect = yes
			}
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	nomadic_empire_decision = {
		potential = {
			government = tribal_chiefdom
			NOT = {
				has_country_modifier = nomadic_empire
			}
			capital_scope = { 
				OR = {
					is_in_region = mobei_region
					is_in_region = modong_region
					is_in_region = scythia_region
					is_in_region = sarmatia_asiatica_region
					is_in_region = sarmatia_europea_region
				}
			}
		}

		allow = {
			has_civil_war = no
			custom_tooltip = {
				text = cannot_stop_migrating_tooltip 
				NOT = {
					any_unit = {
						num_of_migrants > 0
					}
				}
			}
			num_of_cities >= 50
		}
		
		effect = {
			add_country_modifier = {
   				name = nomadic_empire
   				duration = -1
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
}