country_decisions = {
	
	zhuzibaijia_rujia = {
		
		potential = {
			has_variable = zhuzibaijia
			NOT = {
			country_culture_group = chinese
			has_country_modifier = qin_buff
			has_variable = rujia
				}
			}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 1
			}
			treasury >= 100
			political_influence >= 25
		}
		
		effect = {
		    hidden_effect = {
			remove_country_modifier = fajia_buff
			remove_country_modifier = mojia_buff
			remove_country_modifier = daojia_buff
			remove_variable = fajia
			remove_variable = daojia
			remove_variable = mojia
			}
			set_variable = rujia
			add_treasury = -100
			add_stability = -10
			add_political_influence = -25
			add_country_modifier = {
   			name = rujia_buff
   			duration = -1
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	zhuzibaijia_fajia = {
		
		potential = {
			has_variable = zhuzibaijia
			NOT = {
			country_culture_group = chinese
			has_country_modifier = qin_buff
			has_variable = fajia
				}
		}

		allow = {
			current_ruler = {
				charisma >= 5
				finesse >= 6
			}
			treasury >= 250
			political_influence >= 50
		}
		
		effect = {
		    hidden_effect = {
			remove_country_modifier = rujia_buff
			remove_country_modifier = mojia_buff
			remove_country_modifier = daojia_buff
			remove_variable = rujia
			remove_variable = daojia
			remove_variable = mojia
			}
			set_variable = fajia
			add_treasury = -250
			add_stability = -40
			add_political_influence = -50
			add_country_modifier = {
   			name = fajia_buff
   			duration = -1
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	zhuzibaijia_mojia = {
		
		potential = {
			has_variable = zhuzibaijia
			NOT = {
			country_culture_group = chinese
			has_country_modifier = qin_buff
			has_variable = mojia
				}
		}

		allow = {
			current_ruler = {
				charisma >= 4
				finesse >= 5
			}
			treasury >= 200
			political_influence >= 30
		}
		
		effect = {
		    hidden_effect = {
			remove_country_modifier = rujia_buff
			remove_country_modifier = fajia_buff
			remove_country_modifier = daojia_buff
			remove_variable = rujia
			remove_variable = daojia
			remove_variable = fajia
			}
			set_variable = mojia
			add_treasury = -200
			add_stability = -15
			add_political_influence = -30
			add_country_modifier = {
   			name = mojia_buff
   			duration = -1
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	zhuzibaijia_daojia = {
		
		potential = {
			has_variable = zhuzibaijia
			NOT = {
			country_culture_group = chinese
			has_country_modifier = qin_buff
			has_variable = daojia
				}
		}

		allow = {
			current_ruler = {
				charisma >= 4
				finesse >= 4
			}
			treasury >= 150
			political_influence >= 20
		}
		
		effect = {
		    hidden_effect = {
			remove_country_modifier = rujia_buff
			remove_country_modifier = fajia_buff
			remove_country_modifier = mojia_buff
			remove_variable = rujia
			remove_variable = mojia
			remove_variable = fajia
			}
			set_variable = daojia
			add_treasury = -150
			add_stability = -15
			add_political_influence = -20
			add_country_modifier = {
   			name = daojia_buff
   			duration = -1
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	enter_zhuzibaijia_chinese = {
		
		potential = {
		    country_culture_group = chinese
			NOT = {
			has_variable = zhuzibaijia
			}
		}

		allow = {
			current_ruler = {
				charisma >= 1
				finesse >= 1
			}
		}
		
		effect = {
		    set_variable = zhuzibaijia
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	enter_zhuzibaijia_baipu = {
		
		potential = {
		    country_culture_group = pu
			NOT = {
			has_variable = zhuzibaijia
			}
		}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 3
			}
		}
		
		effect = {
		    set_variable = zhuzibaijia
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	enter_zhuzibaijia_baiyue = {
		
		potential = {
		    country_culture_group = yue
			NOT = {
			has_variable = zhuzibaijia
			}
		}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 3
			}
		}
		
		effect = {
		    set_variable = zhuzibaijia
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	enter_zhuzibaijia_rongdi = {
		
		potential = {
		    country_culture_group = rong
			NOT = {
			has_variable = zhuzibaijia
			}
		}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 3
			}
		}
		
		effect = {
		    set_variable = zhuzibaijia
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	enter_zhuzibaijia_yi = {
		
		potential = {
		    country_culture_group = yi
			NOT = {
			has_variable = zhuzibaijia
			}
		}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 3
			}
		}
		
		effect = {
		    set_variable = zhuzibaijia
		}
		
		ai_will_do = {
			factor = 100
		}
	}
}