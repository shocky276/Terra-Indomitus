country_decisions = {
	
	abolish_shangyang_low = {
		
		potential = {
			has_variable = china_is_unified
			has_country_modifier = qin_buff
			}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 4
			}
			treasury >= 100
			political_influence >= 25
		}
		
		effect = {
			add_treasury = -100
			add_stability = -10
			add_political_influence = -25
			set_variable = abolish_shangyang_low
			remove_country_modifier = qin_buff
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	burn_books_buried_Confucian_scholar = {
		
		potential = {
			has_variable = china_is_unified
			has_country_modifier = qin_buff
			NOT = {
			has_variable = fenshukengru
			}
		}

		allow = {
			treasury >= 50
			political_influence >= 10
		}
		
		effect = {
			add_treasury = -50
			add_stability = -20
			add_political_influence = -10
			set_variable = fenshukengru
			set_variable = {
				name = ruzhejiesheng
			}
			add_country_modifier = {
   			name = fenshukengru_buff
   			duration = 10950
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	che_tonggui_shu_tongwen = {
		
		potential = {
			has_variable = china_is_unified
			NOT = {
			has_variable = chetongguishutongwen
			}
		}

		allow = {
			current_ruler = {
				charisma >= 3
				finesse >= 3
			}
			treasury >= 200
			political_influence >= 50
		}
		
		effect = {
			add_treasury = -200
			add_stability = -20
			add_political_influence = -50
			set_variable = chetongguishutongwen
			add_country_modifier = {
   			name = chetongguishutongwen_buff
   			duration = 18250
   		    }
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	yuminxiuxi = {
		
		potential = {
			has_variable = china_is_unified
				NOT = {
					has_variable = yuminxiuxi
				}
			}

		allow = {
			current_ruler = {
				charisma >= 5
				finesse >= 5
			}
			treasury >= 50
			political_influence >= 20
		}
		
		effect = {
			add_treasury = -50
			add_stability = -5
			add_political_influence = -20
			set_variable = yuminxiuxi
			add_country_modifier = {
   			name = yuminxiuxi_buff
   			duration = 18250
   		    }
		}
		
		ai_will_do = {
			factor = 10
		}
	}
	
	reenabling_shangyang_low = {
		
		potential = {
			has_variable = china_is_unified
			has_variable = abolish_shangyang_low
			NOT = {
			has_country_modifier = qin_buff
				}
			}

		allow = {
			current_ruler = {
				charisma >= 5
				finesse >= 6
			}
			treasury >= 100
			political_influence >= 25
		}
		
		effect = {
			add_treasury = -100
			add_stability = -10
			add_political_influence = -25
			remove_variable = abolish_shangyang_low
			add_country_modifier = {
				name = qin_buff
				duration = -1
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}

	restitution_rujia_jinjian = {
		
		potential = {
			has_variable = china_is_unified
			has_variable = fenshukengru
			NOT = {
			has_country_modifier = qin_buff
				}
			}

		allow = {
			current_ruler = {
				charisma >= 5
				finesse >= 6
			}
			treasury >= 100
			political_influence >= 25
		}
		
		effect = {
			add_treasury = -100
			add_stability = -10
			add_political_influence = -25
			remove_variable = ruzhejiesheng
			remove_variable = fenshukengru
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
}