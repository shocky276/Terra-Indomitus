namespace = teng_startup

teng_startup.1 = { #滕开局
	
	type = country_event
	title = "teng_startup.1.t"
	desc = "teng_startup.1.desc"
	picture = china_political
	goto_location = root
	left_portrait = current_ruler
	fire_only_once = yes

	trigger = {
		tag = TNG
	}
	
	immediate = {
    set_variable = chinese_country_named
	set_variable = zhuzibaijia
	    trigger_event = {
	    id = game_start_introduce.1
	    }
   		add_country_modifier = {
   			name = rujia_buff
   			duration = -1
   		}
	}
	
	option = {
		name = teng_startup.1.a
	}
	
}