namespace = qin_startup

qin_startup.1 = { #秦开局buff
	
	type = country_event
	title = "qin_startup.1.t"
	desc = "qin_startup.1.desc"
	picture = china_political
	goto_location = root
	left_portrait = current_ruler
	fire_only_once = yes

	trigger = {
		tag = QIN
	}
	
	immediate = {
    set_variable = chinese_country_named
	set_variable = zhuzibaijia
	    trigger_event = {
	    id = game_start_introduce.1
	    }
   		add_country_modifier = {
   			name = qin_buff
   			duration = -1
   		}
   		add_country_modifier = {
   			name = jungjz_buff
   			duration = -1
   		}
		area:hanzhong_area = { formable_area_province_effect = yes }
		area:hongnong_area = { formable_area_province_effect = yes }
		area:hedong_area = { formable_area_province_effect = yes }
	}
	
	option = {
		name = qin_startup.1.a
	}
	
}