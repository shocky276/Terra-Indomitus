namespace = han_startup

han_startup.1 = { #韩开局buff
	
	type = country_event
	title = "han_startup.1.t"
	desc = "han_startup.1.desc"
	picture = china_political
	goto_location = root
	left_portrait = current_ruler
	fire_only_once = yes

	trigger = {
		tag = HAN
	}
	
	immediate = {
		set_variable = chinese_country_named
		set_variable = zhuzibaijia
	    trigger_event = {
	    id = game_start_introduce.1
	    }
   		add_country_modifier = {
   			name = fajia_buff
   			duration = -1
   		}
   		add_country_modifier = {
   			name = han_buff
   			duration = 17000
   		}
		area:yingchuan_area = { formable_area_province_effect = yes }
	}
	
	option = {
		name = han_startup.1.a
	}
	
}